{{!-- LCARS-themed limited view for STA 2e starships and small craft.
     Shown to players who don't own the actor. Mirrors the structure of
     limited-ship.hbs but with the LCARS color-scheme class applied.
--}}
<div class="character-sheet sta-lcars {{lcarsSchemeClass}}">
  <div class="split-section">
    <div class="top-left-column">
      <div class="image-field">
        <img class="profile-img" src="{{actor.img}}" alt="{{actor.name}}" data-edit="img" title="{{actor.name}}" />
      </div>
    </div>

    <div class="top-right-column">
      <div class="name-field">
        <label class="title" for="limited-name">{{localize 'sta.actor.character.name'}}</label>
        <input name="name" id="limited-name" class="text-entry" type="text" value="{{actor.name}}" placeholder="{{localize 'sta.actor.character.name'}}" />
      </div>

      <label class="title" for="limited-designation">{{localize 'sta.actor.starship.designation'}}</label>
      <input name="system.designation" id="limited-designation" class="text-entry" type="text" value="{{actor.system.designation}}" placeholder="{{localize 'sta.actor.starship.designation'}}" />
      {{#if showProseMirror}}
        <div class="title">{{localize 'sta.actor.note.title'}}</div>
        <div class="note2e">
          <prose-mirror
            name="system.notes"
            document-uuid="{{actor.uuid}}"
            value="{{actor.system.notes}}"
            collaborate="true"
            toggled="false"
            {{#unless editable}}disabled{{/unless}}>
            {{{enrichedNotes}}}
          </prose-mirror>
        </div>
      {{/if}}
    </div>
  </div>
</div>
